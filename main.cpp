#include <iostream>
// #include <vector>

#include "board.h"
#include "turn.h"

int main() {
     // Board v(4,7);

     // v.print();

     // Turn q("man","too");
     // std::cout << q.get() << std::endl;
     // q.switchTurn();
     // std::cout << q.get() << std::endl;
     // q.switchTurn();
     // std::cout << q.get() << std::endl;


     // std::vector<std::vector<int*>> l;
     // l.resize(4);
     // for (auto &h: l) {
          // h.resize(7,nullptr);
     // }

     return 0;
}