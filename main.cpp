#include <iostream>
// #include <vector>

#include "board.h"

int main() {
     Board v(4,7);

     v.print();


     // std::vector<std::vector<int*>> l;
     // l.resize(4);
     // for (auto &h: l) {
          // h.resize(7,nullptr);
     // }

     return 0;
}